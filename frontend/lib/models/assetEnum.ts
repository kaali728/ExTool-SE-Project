export enum STATUS {
  CONFIRMED = "Confirmed",
}

export enum ENGINE {
  RUNNING = "Running",
  STOPED = "Stopped",
}

export enum ASSET_PICK_DROP {
  PICKUP = "Pick up",
  DROP_OFF = "Drop off",
  PICKEDUP = "Picked up",
  DROPEDOFF = "Dropped off",
  ASSET_CREATED = "Asset created",
  AWAITING_PAYMENT = "Awaiting Payment",
}
